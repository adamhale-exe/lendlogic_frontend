// import { cookies } from "next/headers";

// // function that saves question we are on and chosen answer
// export const setCookie = async (num, answer) => {
//   cookies().set(`question${num}`, answer);
// };
